# Forgejo grape — PLACEHOLDER

The real Forgejo grape (docker-compose + grape-forgejo.md + .env.example)
lands from the VERIFIED runbook only — the steps already proven on a live
box (native PostgreSQL role/db, Traefik labels for the git subdomain,
OIDC auth source via `forgejo admin auth add-oauth`).

Do not invent the compose file here. Until the runbook is verified and
folded in, this folder is intentionally empty of installable content.
